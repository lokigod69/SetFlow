# NEXT STEP — SETFLOW/main — updated 2026-07-17 (checkpoint 8)

## FOR YOU
**v2 polish wave is in and live.** Refresh the app tab (or double-click **SETFLOW** on your Desktop if it's not running) and you'll see:
- The composer collapses to a one-line **brief** once a set loads — click it to edit.
- **Drag the ⠿ handle** on any track to reorder — transitions re-validate instantly.
- Spotify exports now stamp the **vibe + energy arc into the playlist description**.
- Stubborn unresolved tracks get a **second brain replacement round**.

Two things only you can do:
1. **Listen test (still the big one):** architect a set, export it, mix/listen. Tell me what felt wrong (order, energy, transitions) — I tune the brain prompts from that.
2. **GetSongBPM:** if a key-activation email arrives, say so; otherwise I re-probe next session.

## PASTE THIS
Resume SETFLOW/main under protocol-os.
Read protocol/PROTOCOL.md and this file, plus memory/INDEX.md and memory/STATE.md.
Verify: STOP the app server first (verify needs port 8321 free — see STATE known problems; kill by port, relaunch SetFlow.cmd after), then `npm run verify` in D:\CODING\SetFlow — expect 8 PASS + 2 PARTIAL.
Then per memory/STATE.md → Next actions: collect the user's listening-test feedback and tune brain prompts; re-probe the GetSongBPM key (expect 401 until their crawler verifies). Session 4 landed the v2 polish wave (compact composer, 2-round replace, playlist description, drag-to-reorder) + 6 adversarial-review fixes — don't redo. Optional: live-verify playlist description via one re-export; cover art is parked (needs re-auth scope). Mode 1.
Work in long runs; checkpoint and Status Block only per protocol-os Iron Rules.
