import { nanoid } from 'nanoid'; export type Job={id:string;status:'running'|'done'|'error';stage:string;progress:number;result?:unknown;error?:string};const jobs=new Map<string,Job>();export function runJob(fn:(set:(stage:string,progress:number)=>void)=>Promise<unknown>):Job{const job:Job={id:nanoid(),status:'running',stage:'queued',progress:0};jobs.set(job.id,job);void fn((stage,progress)=>{job.stage=stage;job.progress=progress;}).then(result=>Object.assign(job,{status:'done' as const,stage:'done',progress:1,result})).catch(e=>Object.assign(job,{status:'error' as const,stage:'error',error:e instanceof Error?e.message:String(e)}));return job;}export const getJob=(id:string)=>jobs.get(id);
