import type { SetDocument, SpotifyPlaylistLink } from '@setflow/shared'; import { SpotifyClient } from './client.js';
export async function createOrUpdatePlaylist(doc:SetDocument, optionId:string, client:SpotifyClient):Promise<SpotifyPlaylistLink>{const option=doc.options.find(x=>x.id===optionId); if(!option)throw new Error('Option not found'); if(process.env.SETFLOW_MOCK==='1')return {playlistId:'mock-playlist',url:'https://open.spotify.com/playlist/mock-playlist',optionId,lastSyncedAt:new Date().toISOString()}; const uris=option.trackIds.map(id=>doc.pool.find(t=>t.id===id)?.spotify?.uri).filter((x):x is string=>Boolean(x)); let id=doc.spotifyPlaylist?.playlistId,url=doc.spotifyPlaylist?.url; if(!id){const me=await client.fetch('/me'); if(!me.ok)throw new Error('Spotify profile unavailable'); const user=await me.json() as {id:string}; const r=await client.fetch(`/users/${user.id}/playlists`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:`SETFLOW - ${doc.name}`,public:false})}); if(!r.ok)throw new Error('Playlist creation failed'); const j=await r.json() as {id:string;external_urls:{spotify:string}}; id=j.id;url=j.external_urls.spotify;} const r=await client.fetch(`/playlists/${id}/items`,{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({uris})}); if(!r.ok)throw new Error('Playlist update failed'); return {playlistId:id!,url:url!,optionId,lastSyncedAt:new Date().toISOString()};}
