import type { SetDocument } from '@setflow/shared';import {option,tracks}from './util.js';
const x=(s:string)=>s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&apos;'}[c]!));
export function rekordbox(doc:SetDocument,id:string){const o=option(doc,id),ts=tracks(doc,o);return `<?xml version="1.0" encoding="UTF-8"?><DJ_PLAYLISTS Version="1.0.0"><COLLECTION Entries="${ts.length}">${ts.map((t,i)=>`<TRACK TrackID="${i+1}" Name="${x(t.title)}" Artist="${x(t.artist)}" TotalTime="${Math.round((t.durationMs?.value??0)/1000)}" AverageBpm="${t.bpm.value}" Tonality="${x(t.key.value)}" Location="${x(t.localFile?.path??t.spotify?.url??'')}"/>`).join('')}</COLLECTION><PLAYLISTS><NODE Type="0" Name="SETFLOW"><NODE Type="1" Name="${x(doc.name)}" KeyType="0" Entries="${ts.length}">${ts.map((_,i)=>`<TRACK Key="${i+1}"/>`).join('')}</NODE></NODE></PLAYLISTS></DJ_PLAYLISTS>`;}
