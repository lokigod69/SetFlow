import type { SetDocument } from '@setflow/shared';import {option,tracks}from './util.js';
export function setsheet(doc:SetDocument,id:string){const o=option(doc,id);return {setName:doc.name,date:doc.createdAt,option:o,tracks:tracks(doc,o).map((t,i)=>({n:i+1,artist:t.artist,title:t.title,mix:t.mix,bpm:t.bpm.value,key:t.key.value,energy:t.energy.value,durationMs:t.durationMs?.value??null})),transitions:o.transitions.map((t,i)=>({n:i+1,camelotPath:t.camelotPath,bpmDelta:t.bpmDeltaPercent,read:t.bpmRead,blend:t.blend,note:t.note,warnings:t.warnings})),arc:o.predictedEnergies,targetCurve:doc.targetCurve};}
